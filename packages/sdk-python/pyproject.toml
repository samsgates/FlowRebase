[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "flowrebase-sdk"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["httpx>=0.28,<1"]
description = "Python SDK for FlowRebase"
license = {text = "Apache-2.0"}
